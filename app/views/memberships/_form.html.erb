<%= form_with(model: membership) do |form| %>
  <% if membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
        <% membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :beer_club_id, style: "display: block" %>
    <%= form.select :beer_club_id, options_for_select(@beer_club.map { |beer_club| [beer_club.name, beer_club.id] }) %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id, options_for_select(@users.map { |user| [user.username, user.id] }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
